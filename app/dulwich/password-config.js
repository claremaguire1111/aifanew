// This file contains a direct password configuration
// It's a workaround for cases where environment variables aren't properly passed to the client
// This password is matched with the one in .env.local and is used for the Dulwich gallery page

// This password should match the DULWICH_PASSWORD in .env.local
export const DULWICH_PASSWORD = 'Dulwich25';